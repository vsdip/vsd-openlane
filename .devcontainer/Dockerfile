# Minimal working Dockerfile using efabless/openlane:v0.21
FROM efabless/openlane:v0.21
SHELL ["/bin/bash", "-lc"]

ENV LANG=C LC_ALL=C DISPLAY=:1
WORKDIR /openLANE_flow

# Just start a shell; no extra packages, no EPEL
CMD ["/bin/bash"]
