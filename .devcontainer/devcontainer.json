{
  "name": "Openlane Workshop",
  "build": { "dockerfile": "Dockerfile" },
  "overrideCommand": false,
  "remoteUser": "root",
  "containerEnv": {
    "DISPLAY": ":1",
    "PDK_ROOT": "/workspaces/work/pdks"
  },
  "postCreateCommand": "bash -lc 'if [ ! -e /openLANE_flow ]; then ln -s \"$PWD\" /openLANE_flow; fi && echo OPENLANE_FLOW_LINK_OK'"
}
